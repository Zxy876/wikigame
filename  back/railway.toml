[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"

[[services]]
name = "web"
[[services.ports]]
port = 8000
protocol = "http"
